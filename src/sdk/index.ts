/**
 * @deprecated — This file is no longer used.
 *
 * The SDK has moved to a standalone package: tracer-sdk/
 * Import it via: import tracer from 'tracer-sdk';
 *
 * This re-export exists only for backwards compatibility with any
 * remaining internal references.
 */
export { default } from "tracer-sdk";
export type {
  TracerInitOptions,
  TracerEvent,
  TracerEventType,
  TracerRect
} from "tracer-sdk";
